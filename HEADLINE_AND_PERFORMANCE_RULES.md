# 見出し・可読性・表示速度の恒久ルール

最終更新: 2026-09-01

この文書は `SITE_OPERATING_PRINCIPLES.md` の補足規則です。今後の全ページ・全ブランド・比較ページ・ブログで適用します。

## 見出しの基本ルール

- 見出しになる情報は、**その端末で無理なく表示できる最大サイズより少し小さい程度**を基本値とする。
- 本文と同程度の文字サイズ・色・太さにして、見出しの存在感を失わせない。
- 見出しは本文より明確に大きく、原則として濃いネイビー等の高コントラスト色・太字・明朝系などで階層を示す。
- 長い見出しは意味単位で改行し、文字を過度に縮小しない。必要ならカードの段組みや高さを変更する。
- 「3つの約束」のようなセクション見出しは、本文より大幅に大きくし、端末幅に応じて最大限読みやすくする。
- カード内見出しも本文と明確に差をつける。文字サイズ・色・ウェイトの3点のうち最低2点以上で明確な階層差を作る。

## 表示速度・スクロール性能のルール

- `MutationObserver` をページ全体の `#root` / `body` に常時設置し、同じDOMを書き換える処理を繰り返してはいけない。
- DOMを書き換える処理を `MutationObserver` から再度呼び出し、自己発火ループを作ってはいけない。
- 初期化待ちは原則として bounded retry（回数制限付きリトライ）を使用し、要素を発見したら停止する。
- レスポンシブは可能な限りCSS media queryで行い、resize時のJavaScriptはdebounceして必要最小限にする。
- 同じセクションへ複数のhotfixスクリプトを重ねない。既存修正を統合し、読み込み本数を減らす。
- スクロール中にレイアウト計算・DOM再構築を繰り返す処理を置かない。
- 見た目の修正で性能を悪化させた場合は不合格とする。

## 第1セクションの具体的な受入基準

- 「一人ひとりに合わせるための、3つの約束」は画面内で主見出しとして十分に目立つ。
- 3枚の約束カードの見出しは本文より明らかに大きく、濃いネイビー・太字で表示する。
- カード本文は見出しより小さく、やや淡い色で表示して情報階層を明確にする。
- iPad mini / iPad Air / iPhone XS Max 等で、見出しを小さくして3列を無理に維持しない。
- 「把／握」「サポー／ト」「つなが／る」などの語中分断を禁止する。
- セクション修正後、スクロールが引っかかる・表示が遅い場合は完成扱いにしない。
