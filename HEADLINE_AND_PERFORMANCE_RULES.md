# 見出し・可読性・表示速度の恒久ルール

最終更新: 2026-09-04

この文書は `SITE_OPERATING_PRINCIPLES.md` の補足規則です。今後の全ページ・全ブランド・比較ページ・ブログで適用します。

## 見出しの基本ルール

- 見出しになる情報は、**その端末で無理なく表示できる最大サイズより少し小さい程度**を基本値とする。
- 本文と同程度の文字サイズ・色・太さにして、見出しの存在感を失わせない。
- 見出しは本文より明確に大きく、原則として濃いネイビー等の高コントラスト色・太字・明朝系などで階層を示す。
- **セクション見出し・主要コピー・カード見出しは中央揃えを基本**とする。左揃えを採用する場合は、写真横の説明など、明確なレイアウト上の理由がある場合に限る。
- 長い見出しは意味単位で改行し、文字を過度に縮小しない。必要ならカードの段組みや高さを変更する。
- 横幅に余裕があるのに、カード数に合わせるためだけに4列・3列を固定して、見出しを不自然に細かく改行してはいけない。**日本語の自然な組版を優先し、PCでも2×2や2列へ変更する。**
- 「3つの約束」のようなセクション見出しは、本文より大幅に大きくし、端末幅に応じて最大限読みやすくする。
- カード内見出しも本文と明確に差をつける。文字サイズ・色・ウェイトの3点のうち最低2点以上で明確な階層差を作る。
- 「把／握」「サポー／ト」「つなが／る」のような語中分断を禁止する。改行は文節・意味単位で行う。

## 全端末・全画面幅へのレスポンシブ対応ルール（絶対ルール）

- サイトは特定機種だけに合わせて作らない。**スマートフォン・タブレット・ノートPC・デスクトップPCの実際の表示幅全域で自然に成立することを必須条件**とする。
- スマートフォンとタブレットは、**縦向き・横向きの両方**を必ず考慮する。端末名ではなく CSS viewport 幅を基準に設計する。
- 代表的な確認幅だけでなく、ブレークポイントとブレークポイントの間の幅でも崩れない流動的なレイアウトにする。特定端末専用の数値調整だけで完成扱いにしない。
- 原則として `width:100%`、`max-width`、`minmax()`、`clamp()`、flex/grid の自動折り返し等を用い、固定幅による横はみ出しを作らない。
- **横スクロールが発生する状態、文字・カード・画像・CTAが画面外へはみ出す状態は端末を問わず不合格**とする。
- Safe Area、ブラウザUI、縦横回転による幅変化を考慮し、モバイル固定CTAやヘッダーも左右が欠けないようにする。
- 文字の可読性を優先する。画面内へ押し込むためだけに見出し・本文を過度に縮小してはいけない。
- 現在の列数で文字が読みにくくなる場合のみ、3列→2列、2列→1列などカード配列を変更する。**まず文章を自然に読める幅を確保し、その結果として列数を決める。**
- カード数が4枚だから4列、6枚だから3列という機械的な配列を禁止する。カード内容・見出し長・端末幅を基準に列数を決める。
- 見出しや本文に `<br>` を入れて端末ごとに無理やり形を合わせる方法は原則禁止する。明確な意味区切りとして全幅で自然に成立する場合だけ使用する。
- CSSによる自然改行を基本とし、手動改行が必要な場合も**句・文節・意味のまとまり**でのみ改行する。
- 「大学受／験」「通信制高／校」「サポー／ト」のような単語途中の分断、助詞だけが次行へ孤立する改行、句読点だけが行頭へ回る組版を避ける。
- 横幅に十分な余白がある場合は、文章を不必要に細い `max-width` へ押し込まず、その端末で読みやすい行長まで広げる。
- PC・タブレット横向きでは余白を活用し、スマホ・タブレット縦向きではカードの列数を減らすなど、**同じ情報階層を保ったまま配置だけを最適化**する。
- レスポンシブ対応はCSS media/container queryを基本とし、端末判定User-Agentや大量のJavaScript hotfixへ依存しない。

## 背景・カード・文字の可読性ルール（絶対ルール）

- **背景色・カード背景・文字色・ボタン色・リンク色の組み合わせは、見た瞬間に読みやすい十分なコントラストを確保する。** 薄い背景に薄いグレー文字、濃色背景に低コントラスト文字を置かない。
- 本文は白〜淡色背景では濃いネイビーまたは濃いグレーを基本とし、補足文でも薄くしすぎない。
- 濃色カード・濃色セクション上の本文は原則白または白に近い色とし、透過率を下げすぎない。重要見出し・CTAは背景との境界が明確であること。
- 写真やグラデーションの上へ直接文字を置く場合は、文字の背後に十分なオーバーレイ・面・影・余白を設け、写真の明暗に可読性を左右されないようにする。
- カードの境界が背景に埋没しないよう、背景差・枠線・影・余白のうち必要なものを使ってカード領域を明確にする。
- ボタンは通常時・ホバー時・フォーカス時のいずれも文字が読めること。白文字を明るすぎる背景に載せない。
- ピンク・ゴールド・ブルー・グリーン等のアクセントカラーは装飾や強調に使い、長文本文の主要文字色として可読性を落とさない。
- 小さい補足文字ほどコントラストを高める。文字を小さくしたうえで色も薄くする二重の可読性低下を禁止する。
- 明るさ・色相だけに意味を依存させず、見出し・太字・枠・ラベル等でも情報階層を表現する。
- 実機または同等viewportで、背景と文字が溶ける・カードの境界が分からない・CTAが背景に埋もれる箇所が1つでもあれば完成扱いにしない。

## レスポンシブ受入テストの最低条件

- 小型スマホ幅、標準スマホ幅、大型スマホ幅で確認する。
- 各スマホ幅について縦向き相当と横向き相当の幅で確認する。
- 小型タブレット（iPad mini級）、標準タブレット、大型タブレット相当の幅で確認する。
- 各タブレットについて縦向き相当と横向き相当の幅で確認する。
- 小型ノートPC、一般的なノートPC、フルHD級以上のデスクトップ幅で確認する。
- 各幅で、見出し・本文・カード・画像・表・CTA・ヘッダー・フッター・固定要素について、横はみ出し、文字つぶれ、不自然な改行、過密、過大な空白がないことを確認する。
- 各幅で、背景・カード・本文・補足文字・リンク・ボタンのコントラストと視認性を確認する。
- 幅を連続的に変更しても、特定の中間幅だけ崩れる箇所がないことを確認する。

## サービス訴求の基本ルール

- 主力の専門領域は「英検・大学受験の文系科目・不登校・通信制高校」とする。
- **大学受験は文系受験生限定ではない。専門は英語・国語系・社会系であり、理系受験生の英語・国語系・社会系も専門範囲に含む。** 「文系大学受験が専門」という誤解を招く表現は禁止する。
- ただし、主力4領域以外を「その他」として弱く扱わない。高校受験、中学受験（対応科目の範囲を明記）、定期テスト・内申、学校補習、さかのぼり学習、中高一貫校サポート等も、対象家庭が自分向けだと判断できる独立した導線で積極的に案内する。
- 総合サイトでは、4つの専門ブランドへの導線と15コース全体への導線を両立させる。

## 表示速度・スクロール性能のルール

- `MutationObserver` をページ全体の `#root` / `body` に常時設置し、同じDOMを書き換える処理を繰り返してはいけない。
- DOMを書き換える処理を `MutationObserver` から再度呼び出し、自己発火ループを作ってはいけない。
- 初期化待ちは原則として bounded retry（回数制限付きリトライ）を使用し、要素を発見したら停止する。
- レスポンシブは可能な限りCSS media queryで行い、resize時のJavaScriptはdebounceして必要最小限にする。
- 同じセクションへ複数のhotfixスクリプトを重ねない。既存修正を統合し、読み込み本数を減らす。
- スクロール中にレイアウト計算・DOM再構築を繰り返す処理を置かない。
- 見た目の修正で性能を悪化させた場合は不合格とする。
