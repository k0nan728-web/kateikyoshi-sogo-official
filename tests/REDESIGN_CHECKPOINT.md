# 全面再設計・再開記録

## 2026-09-14 追加レビュー・今回の到達点（最優先）

基点remote: 5dd91f42b6597cc628b15f6716560a68fb3d284a。local e48a059109fc30e0dd041e01c8e83dd4ced129f1と同一tree bca974c629aa7c78046a0cb3a43f7d4d7387bd39、開始時worktree cleanを確認。この追記を含むコミットが次の再開地点。

### 実施内容

- 旧Manusを実ブラウザで開き、トップ/講師/特徴/コース/実績/不登校事例/早稲田事例/授業画面/料金/キャンセル/開始フロー/お問い合わせ/保護者の声を目視。URLのhash移動直後はアニメーション待ちで白いことがあり、後続のスクリーンショットで内容表示後に評価した。
- 契約欄: 320pxで矢印だけが前の行に残る折返しを検出。container幅34rem以下では縦の相談→講師本人→授業図へ。320/360と768で目視、32px線画の巨大化なしを確認。
- 実績数字: 390pxで3つの大きな区画が続き一覧性が弱かったため、600px以下を数字右・ラベル左のコンパクトな行へ。拡大時はauto-fitで列を減らす。390px目視済み。
- 不登校事例: 旧Manusに比べ色による区別が弱いため、2事例を緑/青の淡色背景＋強いカテゴリー/成果へ。390pxで両事例の状態→支援→成果を目視済み。
- ブランド: hover時に全ブランドが同じアイボリーへ変わる既存指定を除去。固有色を保った枠の強調へ。1366の4列と390の英検ブランド内容を目視。hover修正後の最終確認は残る。
- ブログ: 装飾矢印が非クリック領域だったため、記事タイトルのリンク領域をarticle全体へ拡張。focus-withinの可視枠追加。リンク操作の再確認は残る。
- 合格到達図: 320で矢印の折返しを検出。契約図と同じ34rem基準で縦の到達図へ。見出し/スタート/現役合格に意味単位追加。修正後の最終目視は残る。
- PC開始フロー: 旧Manusの5段階一覧性が勝っていたため、十分なコンテナ幅66rem以上を白カードなしの横型5段階に変更。1366で全5段階が一画面に収まることを目視。狭幅/拡大時は縦型を維持。
- 最終CTA: 前回追加した上下余白を320/768/1366で目視、無料相談と有料体験の区別を維持。
- 料金計算下の注記、指導特徴の文末を意味単位に調整。
- 文字拡大時の既存 .text-enlarged .semantic-unit が display:inline にしていたため、768px/200%で「その後で」「ありません」の途中折返しが再発していることを目視。inline-block + white-space:normal + max-width:100%へ変更し、収まる単位は保持、収まらない単位のみ内部折返しする。これは最後の編集で、まだ実表示再検証前。

### QAと未完了の区別

前回19幅/全38展開/200%記録は削除せず維持。今回の最新編集まで無条件に合格とみなさない。

- 今回の変更章のみの局所・ページ超過検査: 通常320(client305)、360(client345)、200%320、768(client753)で0。ただし最後の共通semantic-unit編集前。detailsは既定状態、全38展開ではない。
- build PASS: CSS index-BzJ1J4lA.css / JS index-yNP5xP8a.js（実績見出しの意味単位追加および最後のsemantic-unit編集の前）。verify.py PASS、主要責務各1/15料金表維持。料金計算6ケースPASS、diff --check PASS。
- ブラウザ一括回帰は360でfonts-loading待ち停止。1回reload後に実表示を確認し再開。上限やサイト停止と断定しない。その後、再開指示が届いた時点で実行セッション変数tabが消失。最後の共通CSS編集後はブラウザ再接続から必要。
- 今回未完了: 最終編集の200%全38展開、変更章の残り幅、全章の端末別目視完走、計算機/記事リンク/全CTA操作、画像読込と寸法、5観点＋第6比較の全章確定。

### 次回順序

1. このコミットとclean状態を確認。既存19幅を無条件に再実施しない。
2. browser-clientを再接続。プレビュー停止済みならsites-preview start。768×1024/root32/processを最初に開き、意味単位の折返し修正を確認。
3. 共通文字拡大CSS変更の影響を320/768/1024・root32・全38details展開で回帰。長い単位が内部で崩れる場合は当該文章をより自然な短い単位へ分割し、文字を縮めない。
4. 通常幅は契約図/到達図/実績数字/フローなど今回変更した章を中心に、境界付近と指定幅の未検証部分を確認。最終CTAの余白目視を無駄に繰り返さない。
5. 未完了全章目視・5観点・Manus比較表を完了する。途中判定はVISUAL_COMPARISON_20260914.md参照。
6. 今回ユーザーの最新指示はPageShareも更新禁止。候補が仕上がった段階で報告のみ。main/root/本番には一切反映しない。

現時点は完成候補ではなく、100点判定不可。PageShare/main/本番は変更なし。

最終保存前の再buildはPASS: index.html 90.16kB / index-B-pZ35FN.css 33.40kB / index-Cx-aix8m.js 3.95kB。最後の共通semantic-unit編集も含む。git diff --check PASS。実表示QAは上記の未完了を維持する。

## 最新の再開到達点（この追記を優先）

再設計の保存コミット: 52a1d9b21ee1b99cfa35bd0aee8b4f325b8a9b23。その後、開始フローの説明を意味単位に分け、最終CTAの上下余白を追加、直接契約4セルの旧絵文字を統一線画へ置換。

- 最新19幅の全38details展開を完走し、ページ/局所横超過0。結果はredesign-widths.json。iframe指定幅とスクロールバーを除くclientWidthを別々に記録。
- 200%320/768/1024も全38展開・横超過0。redesign-enlarged.json。これらは上記3箇所の目視修正直前の測定。修正後のprocess/contactは320/390/768/1024の200%で横超過0を確認。固定32px線画を入れた契約欄の最終目視・回帰は次回実施。
- 568×320メニューのEnter展開、8リンク、Escape閉鎖（aria-expanded=false）を確認。
- 目視追加: 768×1024キャンセル対比、390×844開始フローと最終CTA、320×844修正後フロー、1024×768料金導入と任意支援/継続特典/兄弟割引、1366×900不登校支援と成果比較、768×1024契約関係図、1366×900授業画面主役の構成。
- ブログ3記事と4専門サイトを公開ブラウザで開き、URLとページタイトルの一致を確認。

### 残る工程・次回開始点

1. 新しい契約アイコンとCTA余白を320/768/1366で目視し、該当章のみ横超過・コントラストを再確認。既に保存済み19幅全体を無条件にやり直さない。
2. 料金計算機下の注記など、文末だけが折り返す箇所の意味単位調整。未変更の文章にも課題があるので、数値QAだけで合格にしない。
3. 未完了の全章目視（特にモバイルの実績校一覧・事例・ブログ、PCのフロー/最終CTA）と画像読み込み/原比率/タップ領域/キーボード/計算機の操作を終える。
4. 下記5観点のレビューを完了し、課題を解消してから候補をPageShareへ。現在のPageShareは旧版のまま。

### 5観点レビュー（途中評価・採点未確定）

- 保護者: 数字、目的別4入口、状態→支援→結果、キャンセル可否は本文を読まなくても分かるようになった。モバイル全章の読了体験は未完了。
- デザイン: 数字主役/人物動画/全幅授業画面/対比/縦フローの形式を実装。ブランドの本文量と章間余白の通覧評価が残る。
- SEO: 既存の正しい見出し・文章・専門サイト導線と教育記事を接続。プレビューnoindexを維持。公式SNS未確認なので非掲載。
- CRO: 無料相談/有料体験、条件と費用を先に提示。全CTAの実操作再検証が残る。
- 実装: 19幅/200%/38detailsの数値検証を保存。実機Safari、全章目視、最新局所修正の検証が残る。

独立QA「本文を読まず価値が理解できるか」は、強み=濃紺の経験数字、対象=4ブランド、成果=校数/資格、方法=授業画面/前中後、費用=数字と比較、開始=5段階、相談=無料CTAを実装・一部目視済み。全ページでの合否は未確定。100点・完成ではない。

### 再開環境の補足

ブラウザとプレビューがセッション切替で消失したため、公式browser-clientで再接続しsites-previewを再開した。全19幅は数幅ごとにJSON保存する方式で回収済み。Python bs4は再開環境で利用できなかったため、今回の局所編集は標準Pythonの対象を限定した文字列編集を使用。サイト依存は変更していない。

## 保存先と基点

- ブランチ: codex/official-parent-rebuild-20260910
- 基点GitHub: 33efb04eef36ee3e5b5b6ae0546cbaebccfc885b
- 今回の保存コミットはこのファイルを追加したコミット（Git履歴参照）。公開中PageShareは以前のa6cb6dd相当であり今回の再設計とは異なる。
- main/root/本番変更禁止。実機確認前に100点・完成と判定しない。

## 実装済み・最終承認前

直接契約の担当関係図、4ブランドの目的・対象・専門・CTA、大学104/高校90/中学18の数字と学校名分類、合格までの到達図、不登校/発達特性の状態→支援→成果、支援アイコン、授業画面主役の全幅構成、保護者の声の成果ラベル、基本料金/有料体験/任意支援/継続特典/兄弟割引の可視化、キャンセル条件対比、5段階開始フロー、実在3記事の教育コラム、最終無料相談CTAを実装。既存15料金表・計算機・文章詳細・画像は維持。

設計比較表: VISUAL_MASTERPLAN.md。A/B/C/D分類とManusから継承する情報理解の原則を記録。

## 根拠確認

旧runtime index-ROriAf5U.js のi3/r3/c3のschools配列を集計。大学104件/高校90件/中学18件で各々重複なし、旧掲載校数に一致。人数や単年度実績ではない。既存の難度別カテゴリーも復元。合格率80%の母数と認定元は未確認のため非掲載を維持。

追加2事例はManus復元版 #futouko の既存掲載内容（不登校→チャット・基礎→英検2級、発達特性→本人のペース→英検3級）から要約。新しい診断・人数・期間を追加していない。

mainの4ブランドindex.htmlで対象/内容/色を確認。独立したロゴ画像はなく、名称を表示。blog/content-hub.jsの実在3記事を使用。slug: eiken-study-roadmap / humanities-exam-priority / restart-study-at-home。SNS設定は4種類ともURL空欄なのでアカウントは非掲載。記事リンク先の実表示確認は未完了。

## QAの正確な状態

現在ソースでbuild PASS（CSS index-CA3aCky6.css、JS index-BzIhB-Ao.js）、verify.py PASS（主要責務各1、15料金表、画像元寸法保持）、料金計算6ケースPASS、git diff --check PASS。

途中版: 320/360/375/390/430で全38details展開・ページ/局所横超過0。1024/1180/1280/1366/1440/1536/1920も同様。568〜844ではフローの番号枠が狭いことを検出し、2.5rem→3.5remへ修正。

200%途中版: 320/1024は横超過0。768は料金数字の局所超過を検出し、料金導入3列固定→auto-fitへ変更。再検証は未完了。

最新ソースで全19幅の一括再検証を開始したが、実行cellが中断され、結果保持セッションも消失した。したがって最新19幅は合格扱いせず未完了とする。旧記録をそのまま現行へ流用しない。

目視済み: 390ブランド冒頭、375合格実績、1366ブランド4列。最終修正後の目視と全体5観点レビューは未完了。

## 次回再開順

1. この保存コミットとローカル差分を確認。実装を作り直さない。
2. 最新19幅を数値検証し、各数幅の完了ごとに結果をファイルへ保存する。38detailsを対象にする。
3. 200%320/768/1024、横向き568×320・タブレット縦横を検証。料金数字/フロー/ブランドの回帰重点。
4. 全章をPC/タブレット/スマホで目視。特に契約図・ブランド対象の折返し・学校一覧・料金・キャンセル・フロー・最終CTAの色と文字コントラスト。問題を修正して影響幅のみ再検証。
5. ブログ3記事/4ブランド/相談リンク・メニュー・計算機を実操作確認。SNSの架空追加は禁止。
6. 保護者/デザイン/SEO/CRO/実装と、本文を読まず価値を理解する独立レビューを記録。
7. QA通過候補のみPageShareへ更新し、Version active/public/表示一致を確認。Safari実機確認待ち。main/rootは変更しない。

## 技術的な問題

- 一時的なフォント読込/iframe ready停滞。1回再読込では解消せず、次の検証ページではfonts-readyとqa-readyを確認して再開できた。harnessにqaPhaseを追加し、待機地点が分かるようにした。サイト本体に回避パッチは追加していない。
- 新設実績sectionと既存proofのid=results重複を構造検証で発見し、新設をschool-resultsへ修正済み。
- ローカルPythonにbs4がなく初回編集スクリプト停止。インストール後成功。編集用スクリプトはscratch、成果物はindex.html/site.css。サイトの依存は変更なし。
- ブラウザ実行結果が失われた場合は、結果未取得を明示し、合格と推定しない。
